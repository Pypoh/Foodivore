<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.foodivore">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Foodivore"
        tools:targetApi="m">
        <activity
            android:name=".ui.food.FoodDetailActivity"
            android:theme="@style/MaterialTheme.NoActionBar" />
        <activity
            android:name=".ui.main.plans.PlansActivity"
            android:theme="@style/MaterialTheme.NoActionBar" />
        <activity
            android:name=".scanner.camera.CameraActivity"
            android:theme="@style/MaterialTheme.NoActionBar" />
        <activity
            android:name=".scanner.camera.DetectorActivity"
            android:theme="@style/MaterialTheme.NoActionBar" />
        <activity
            android:name=".ui.pretest.PreTestActivity"
            android:theme="@style/MaterialTheme.NoActionBar" />
        <activity
            android:name=".ui.auth.AuthActivity"
            android:theme="@style/MaterialTheme.NoActionBar" />
        <activity
            android:name=".ui.splash.SplashScreenActivity"
            android:theme="@style/MaterialTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.auth.InBoardingActivity"
            android:theme="@style/MaterialTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/MaterialTheme.NoActionBar" />
    </application>

</manifest>